# Dragon Tower Game Logs
> These are game logs of the Dragon Tower Game on stake. They are from the hard difficulty setting. I thought it would be good to collect game logs after each loss. There are 512 possible placement patterns. I will add more to this csv master file and also add the game log as a separate csv file. There is no better feeling than to make it to the top and win that 500x multiplier. It just needs to happen more often.

### This AI is successful at predicting the first four tiles about 80% of the time
> Hopefully it will predict more tiles correctly as it is trained on more data